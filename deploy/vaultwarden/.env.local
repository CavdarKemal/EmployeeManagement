# Lokale Test-Konfiguration. NICHT in Produktion verwenden.
# Wird per --env-file .env.local geladen.

DOMAIN=http://localhost:8222
ADMIN_TOKEN=local-dev-token-not-secure
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_FROM=test@local.invalid
SMTP_FROM_NAME=Local Vault
SMTP_USERNAME=test
SMTP_PASSWORD=test
